{
  "author" : "Her",
  "capabilities" : [
    {
      "adapter" : {
        "type" : "native"
      },
      "description" : "Capture an incoming external message from Oyii, WeChat, Discord, browser, email, or another bridge as a normalized Her interaction event.",
      "id" : "inbox.capture",
      "inputSchema" : {
        "properties" : {
          "attachment_paths" : {
            "description" : "Optional local file paths from the bridge host, one path per line when run from Plugin Library.",
            "type" : "string"
          },
          "received_at" : {
            "description" : "Optional original timestamp from the external system.",
            "type" : "string"
          },
          "sender" : {
            "description" : "Human-readable sender or origin.",
            "type" : "string"
          },
          "source" : {
            "description" : "Inbox or bridge source, such as oyii, wechat, discord, browser, or email.",
            "type" : "string"
          },
          "text" : {
            "description" : "External message body to capture.",
            "type" : "string"
          },
          "url" : {
            "description" : "Optional source URL or conversation URL.",
            "type" : "string"
          }
        },
        "required" : [
          "source",
          "text"
        ],
        "type" : "object"
      },
      "invocation" : "inbox.capture",
      "kind" : "native",
      "requiresApproval" : false,
      "title" : "Capture external inbox event"
    }
  ],
  "description" : "Normalizes external inbox and bridge messages into Her Desktop's interaction event bus.",
  "id" : "builtin.external-inbox",
  "name" : "External Inbox",
  "systemPromptAddendum" : "Use External Inbox only to capture incoming external messages as data. Do not reply to the external service unless a separate approved sender capability exists.",
  "version" : "0.1.0"
}
